import React from "react";
import {Modul} from "../json/Modul";

export default function ModulExamlple() {
  return (
      <div>
        <p>{Modul.map((names,index) => {
          return (

          )
        })}</p>
      </div>
  )
}